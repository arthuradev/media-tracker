<UserControl x:Class="MediaTracker.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MediaTracker.ViewModels"
             mc:Ignorable="d"
             xmlns:conv="clr-namespace:MediaTracker.Converters"
             d:DataContext="{d:DesignInstance vm:HomeViewModel}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    MaxWidth="640"
                    Margin="20,0,0,48">

            <!-- Hero title -->
            <TextBlock Text="Your collection"
                       Style="{StaticResource HeroText}"
                       Foreground="{StaticResource TextPrimaryBrush}"/>

            <!-- Tagline -->
            <TextBlock Text="A calm, curated place for everything you watch and play."
                       Style="{StaticResource BodyText}"
                       Margin="0,10,0,0"/>

            <!-- Stats row -->
            <StackPanel Orientation="Horizontal"
                        Margin="0,32,0,0"
                        Visibility="{Binding HasItems, Converter={StaticResource BoolToVis}}">
                <!-- Movies -->
                <Border Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusMedium}"
                        Padding="20,14"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding MovieCount, Mode=OneWay}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Movies"
                                   Style="{StaticResource CaptionText}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Series -->
                <Border Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusMedium}"
                        Padding="20,14"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding SeriesCount, Mode=OneWay}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Series"
                                   Style="{StaticResource CaptionText}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Anime -->
                <Border Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusMedium}"
                        Padding="20,14"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding AnimeCount, Mode=OneWay}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Anime"
                                   Style="{StaticResource CaptionText}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Games -->
                <Border Background="{StaticResource BackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource RadiusMedium}"
                        Padding="20,14">
                    <StackPanel>
                        <TextBlock Text="{Binding GameCount, Mode=OneWay}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Games"
                                   Style="{StaticResource CaptionText}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Empty state -->
            <TextBlock Text="Your library is empty. Add something to get started."
                       Style="{StaticResource CaptionText}"
                       Margin="0,32,0,0"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"/>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" Margin="0,32,0,0">
                <Button Content="+ Add Media"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding AddMediaCommand}"/>

                <Button Content="Browse Library"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding OpenLibraryCommand}"
                        Margin="12,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
